[project]
name = "fly-in"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "arcade>=3.3.3",
    "argparse>=1.4.0",
    "rich>=15.0.0",
    "webcolors>=25.10.0",
]
